<p id="notice"><%= notice %></p>

<h1>Processed Logs</h1>

<table>
  <thead>
    <tr>
      <th>Phase</th>
      <th>All phase done</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @processed_logs.each do |processed_log| %>
      <tr>
        <td><%= processed_log.phase %></td>
        <td><%= processed_log.all_phase_done %></td>
        <td><%= link_to 'Show', processed_log %></td>
        <td><%= link_to 'Edit', edit_processed_log_path(processed_log) %></td>
        <td><%= link_to 'Destroy', processed_log, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Processed Log', new_processed_log_path %>
